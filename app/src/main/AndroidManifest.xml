<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.REORDER_TASKS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Android 11+ package-visibility restriction: without this, PackageManager
         .getPackageInfo(FRAME_PACKAGE, ...) silently fails as if Frame weren't
         installed at all (AppsFilter blocks the query), regardless of Device Owner
         status. Confirmed live 2026-08-22: ReleaseChecker's installed-version check
         always read 0, so every update cycle re-triggered an unnecessary reinstall. -->
    <queries>
        <package android:name="app.carecast.frame" />
    </queries>

    <application
        android:label="CareCast Device Manager"
        android:allowBackup="false"
        android:directBootAware="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:directBootAware="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".DeviceManagerAdminReceiver"
            android:permission="android.permission.BIND_DEVICE_ADMIN"
            android:exported="true"
            android:directBootAware="true">
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin_receiver" />
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".BootReceiver"
            android:exported="true"
            android:directBootAware="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <service
            android:name=".WatchdogService"
            android:exported="false" />

        <receiver
            android:name=".InstallResultReceiver"
            android:exported="false" />

        <receiver
            android:name=".EmergencyReleaseReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="app.carecast.devicemanager.EMERGENCY_RELEASE" />
            </intent-filter>
        </receiver>

    </application>
</manifest>
